version: "3.9"

services:
  bot:
    build: .
    container_name: discord-fishing-bot
    restart: unless-stopped
    environment:
      - DISCORD_TOKEN=${DISCORD_TOKEN}
    volumes:
      - ./data:/app/data
    env_file:
      - .env